//! NYX Graphics Layer [Layer 20]
//! Low-level GPU access and rendering.

pub mod gpu {
    pub struct Device;
    pub struct Buffer;
    pub struct Shader;
}

pub mod rendering {
    pub struct Renderer;
}
