# Nyx Runtime

Runtime modules:

- `runtime/memory`: safe allocation helpers and cleanup scopes
- `runtime/threads`: thread creation helpers
- `runtime/scheduler`: cooperative task scheduler
- `runtime/module_loader`: engine registry loader
