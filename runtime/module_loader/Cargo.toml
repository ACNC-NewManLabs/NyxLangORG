[package]
name = "nyx-module-loader"
version = "0.1.0"
edition = "2021"
authors = ["SURYA SEKHAR ROY"]
description = "Industrial engine registry and module loading for the Nyx Runtime™"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
