fn add_bits(a: int, b: int) -> int {
    return (a & b) + (a | b)
}

fn main() {
    let x = 5
    let y = 3
    let res = add_bits(x, y)
    print(res)
    
    let shift = 1 << 3
    print(shift)
}
