use ml::nn;

fn create(config) {
    return ml::nn::Sequential([
        ml::nn::Conv2D(3, 32, 3, 2, 1),
        ml::nn::AdaptiveAvgPool2d(1, 1),
        ml::nn::Flatten(),
        ml::nn::Linear(1280, 1000, null, "cpu")
    ]);
}
